/**
 * Outer wrapper for forms/content inside {@link AppConfirmation}.
 * Use with AppConfirmation’s default fullWidth + maxWidth so the body fills the dialog.
 */
export const MODAL_FORM_ROOT_CLASS =
  "w-full min-w-0 max-w-full box-border";
